{
  "name": "inventory-tracker-backend",
  "version": "1.0.0",
  "description": "Backend for Smart Inventory Tracker",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "kill-port": "netstat -ano | findstr :5000 && for /f \"tokens=5\" %a in ('netstat -ano ^| findstr :5000') do taskkill /f /pid %a"
  },

  "dependencies": {
    "express": "^4.18.2",
    "mysql2": "^3.6.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}